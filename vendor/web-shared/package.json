{
  "name": "web-shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./autocomplete": "./src/autocomplete.ts",
    "./dateTimeConverter": "./src/dateTimeConverter.ts"
  },
  "devDependencies": {
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "lint": "oxlint && oxfmt --check src && tsc",
    "lint-fix": "oxlint --fix && oxfmt src"
  }
}
